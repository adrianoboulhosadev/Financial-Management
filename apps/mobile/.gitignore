# Generated by `expo prebuild` — this is a CNG project, so the native folders
# are BUILD OUTPUT, not source. Committing them would freeze the native side
# against app.json and the next prebuild would silently disagree with them.
/android
/ios

