DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5434/financial?schema=public"
# Same Redis as the backend: it produces the recurrence/budget-check jobs this
# app consumes, and both publish the live-update pings the SSE stream relays.
REDIS_URL="redis://localhost:6379"
