# Read by the Prisma CLI (migrate/generate/studio) when run from this package.
# Same database the backend and the worker point at.
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5434/financial?schema=public"
