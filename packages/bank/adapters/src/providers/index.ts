import type {
  BankRepository,
  BankQueryRepository,
  CardRepository,
  CardQueryRepository,
} from '@bank/core'

export type { BankRepository, BankQueryRepository, CardRepository, CardQueryRepository }
